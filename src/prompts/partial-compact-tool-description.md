{{INSTRUCTION_POINTER}} Replace one contiguous range, or multiple disjoint ranges with `ranges`, using summaries you write. The originals stay in the session log but are removed from your working view. Use it for stale bulky raw context whose conclusions are durable; do not call it just because a reminder appeared. Prefer one batch call when compacting multiple stale ranges in the current session to save turns and KV cache.
