{{INSTRUCTION_POINTER}} Use this tool only after choosing exact message ranges that are safe to replace with summaries. Prefer `ranges`: each item has `from_message_id`, `to_message_id`, `summary`, and optional `session_id`; omit `session_id` for the current session. Put multiple disjoint stale ranges in one call when you can summarize them safely. The original messages stay in the session log, but future model turns see your summaries instead of those messages.
