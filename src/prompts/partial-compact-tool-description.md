{{INSTRUCTION_POINTER}} Use this tool only after choosing exact current-session message ranges that are safe to replace with summaries. Prefer `ranges`: each item has `from_message_id`, `to_message_id`, and `summary`. Put multiple disjoint stale ranges in one call when you can summarize them safely; stale context does not need to be near the limit if it is unlikely to be useful soon. Use only current-session message IDs. The original messages stay in the session log, but future model turns see your summaries instead of those messages.
